// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/looplj/axonhub/internal/ent/predicate"
	"github.com/looplj/axonhub/internal/ent/rechargerecord"
	"github.com/looplj/axonhub/internal/ent/redemptioncode"
	"github.com/looplj/axonhub/internal/ent/user"
)

// RedemptionCodeQuery is the builder for querying RedemptionCode entities.
type RedemptionCodeQuery struct {
	config
	ctx                      *QueryContext
	order                    []redemptioncode.OrderOption
	inters                   []Interceptor
	predicates               []predicate.RedemptionCode
	withUser                 *UserQuery
	withRechargeRecords      *RechargeRecordQuery
	loadTotal                []func(context.Context, []*RedemptionCode) error
	modifiers                []func(*sql.Selector)
	withNamedRechargeRecords map[string]*RechargeRecordQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RedemptionCodeQuery builder.
func (_q *RedemptionCodeQuery) Where(ps ...predicate.RedemptionCode) *RedemptionCodeQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RedemptionCodeQuery) Limit(limit int) *RedemptionCodeQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RedemptionCodeQuery) Offset(offset int) *RedemptionCodeQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RedemptionCodeQuery) Unique(unique bool) *RedemptionCodeQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RedemptionCodeQuery) Order(o ...redemptioncode.OrderOption) *RedemptionCodeQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUser chains the current query on the "user" edge.
func (_q *RedemptionCodeQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(redemptioncode.Table, redemptioncode.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, redemptioncode.UserTable, redemptioncode.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRechargeRecords chains the current query on the "recharge_records" edge.
func (_q *RedemptionCodeQuery) QueryRechargeRecords() *RechargeRecordQuery {
	query := (&RechargeRecordClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(redemptioncode.Table, redemptioncode.FieldID, selector),
			sqlgraph.To(rechargerecord.Table, rechargerecord.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, redemptioncode.RechargeRecordsTable, redemptioncode.RechargeRecordsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RedemptionCode entity from the query.
// Returns a *NotFoundError when no RedemptionCode was found.
func (_q *RedemptionCodeQuery) First(ctx context.Context) (*RedemptionCode, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{redemptioncode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RedemptionCodeQuery) FirstX(ctx context.Context) *RedemptionCode {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RedemptionCode ID from the query.
// Returns a *NotFoundError when no RedemptionCode ID was found.
func (_q *RedemptionCodeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{redemptioncode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RedemptionCodeQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RedemptionCode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RedemptionCode entity is found.
// Returns a *NotFoundError when no RedemptionCode entities are found.
func (_q *RedemptionCodeQuery) Only(ctx context.Context) (*RedemptionCode, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{redemptioncode.Label}
	default:
		return nil, &NotSingularError{redemptioncode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RedemptionCodeQuery) OnlyX(ctx context.Context) *RedemptionCode {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RedemptionCode ID in the query.
// Returns a *NotSingularError when more than one RedemptionCode ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RedemptionCodeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{redemptioncode.Label}
	default:
		err = &NotSingularError{redemptioncode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RedemptionCodeQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RedemptionCodes.
func (_q *RedemptionCodeQuery) All(ctx context.Context) ([]*RedemptionCode, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RedemptionCode, *RedemptionCodeQuery]()
	return withInterceptors[[]*RedemptionCode](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RedemptionCodeQuery) AllX(ctx context.Context) []*RedemptionCode {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RedemptionCode IDs.
func (_q *RedemptionCodeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(redemptioncode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RedemptionCodeQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RedemptionCodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RedemptionCodeQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RedemptionCodeQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RedemptionCodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RedemptionCodeQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RedemptionCodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RedemptionCodeQuery) Clone() *RedemptionCodeQuery {
	if _q == nil {
		return nil
	}
	return &RedemptionCodeQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]redemptioncode.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.RedemptionCode{}, _q.predicates...),
		withUser:            _q.withUser.Clone(),
		withRechargeRecords: _q.withRechargeRecords.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RedemptionCodeQuery) WithUser(opts ...func(*UserQuery)) *RedemptionCodeQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithRechargeRecords tells the query-builder to eager-load the nodes that are connected to
// the "recharge_records" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RedemptionCodeQuery) WithRechargeRecords(opts ...func(*RechargeRecordQuery)) *RedemptionCodeQuery {
	query := (&RechargeRecordClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRechargeRecords = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RedemptionCode.Query().
//		GroupBy(redemptioncode.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RedemptionCodeQuery) GroupBy(field string, fields ...string) *RedemptionCodeGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RedemptionCodeGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = redemptioncode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RedemptionCode.Query().
//		Select(redemptioncode.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *RedemptionCodeQuery) Select(fields ...string) *RedemptionCodeSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RedemptionCodeSelect{RedemptionCodeQuery: _q}
	sbuild.label = redemptioncode.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RedemptionCodeSelect configured with the given aggregations.
func (_q *RedemptionCodeQuery) Aggregate(fns ...AggregateFunc) *RedemptionCodeSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RedemptionCodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !redemptioncode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RedemptionCodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RedemptionCode, error) {
	var (
		nodes       = []*RedemptionCode{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withUser != nil,
			_q.withRechargeRecords != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RedemptionCode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RedemptionCode{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *RedemptionCode, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRechargeRecords; query != nil {
		if err := _q.loadRechargeRecords(ctx, query, nodes,
			func(n *RedemptionCode) { n.Edges.RechargeRecords = []*RechargeRecord{} },
			func(n *RedemptionCode, e *RechargeRecord) {
				n.Edges.RechargeRecords = append(n.Edges.RechargeRecords, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedRechargeRecords {
		if err := _q.loadRechargeRecords(ctx, query, nodes,
			func(n *RedemptionCode) { n.appendNamedRechargeRecords(name) },
			func(n *RedemptionCode, e *RechargeRecord) { n.appendNamedRechargeRecords(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RedemptionCodeQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*RedemptionCode, init func(*RedemptionCode), assign func(*RedemptionCode, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*RedemptionCode)
	for i := range nodes {
		fk := nodes[i].UsedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "used_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RedemptionCodeQuery) loadRechargeRecords(ctx context.Context, query *RechargeRecordQuery, nodes []*RedemptionCode, init func(*RedemptionCode), assign func(*RedemptionCode, *RechargeRecord)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*RedemptionCode)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rechargerecord.FieldCodeID)
	}
	query.Where(predicate.RechargeRecord(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(redemptioncode.RechargeRecordsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CodeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "code_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RedemptionCodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RedemptionCodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(redemptioncode.Table, redemptioncode.Columns, sqlgraph.NewFieldSpec(redemptioncode.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, redemptioncode.FieldID)
		for i := range fields {
			if fields[i] != redemptioncode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withUser != nil {
			_spec.Node.AddColumnOnce(redemptioncode.FieldUsedBy)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RedemptionCodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(redemptioncode.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = redemptioncode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *RedemptionCodeQuery) Modify(modifiers ...func(s *sql.Selector)) *RedemptionCodeSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// WithNamedRechargeRecords tells the query-builder to eager-load the nodes that are connected to the "recharge_records"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *RedemptionCodeQuery) WithNamedRechargeRecords(name string, opts ...func(*RechargeRecordQuery)) *RedemptionCodeQuery {
	query := (&RechargeRecordClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedRechargeRecords == nil {
		_q.withNamedRechargeRecords = make(map[string]*RechargeRecordQuery)
	}
	_q.withNamedRechargeRecords[name] = query
	return _q
}

// RedemptionCodeGroupBy is the group-by builder for RedemptionCode entities.
type RedemptionCodeGroupBy struct {
	selector
	build *RedemptionCodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RedemptionCodeGroupBy) Aggregate(fns ...AggregateFunc) *RedemptionCodeGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RedemptionCodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RedemptionCodeQuery, *RedemptionCodeGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RedemptionCodeGroupBy) sqlScan(ctx context.Context, root *RedemptionCodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RedemptionCodeSelect is the builder for selecting fields of RedemptionCode entities.
type RedemptionCodeSelect struct {
	*RedemptionCodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RedemptionCodeSelect) Aggregate(fns ...AggregateFunc) *RedemptionCodeSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RedemptionCodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RedemptionCodeQuery, *RedemptionCodeSelect](ctx, _s.RedemptionCodeQuery, _s, _s.inters, v)
}

func (_s *RedemptionCodeSelect) sqlScan(ctx context.Context, root *RedemptionCodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *RedemptionCodeSelect) Modify(modifiers ...func(s *sql.Selector)) *RedemptionCodeSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
